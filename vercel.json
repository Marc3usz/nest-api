{
  "version": 2,
  "buildCommand": "npm run prisma:generate && npm run build",
  "functions": {
    "api/index.ts": {
      "maxDuration": 15,
      "includeFiles": "node_modules/swagger-ui-dist/**/*"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
